{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v trouve >/dev/null 2>&1 || { echo 'trouve not found on PATH; install with `cargo install trouve` or download a release binary from https://github.com/jimsimon/trouve/releases' >&2; exit 1; }; nohup trouve stats \"${CLAUDE_PROJECT_DIR:-.}\" >/dev/null 2>&1 &"
          }
        ]
      }
    ]
  }
}
