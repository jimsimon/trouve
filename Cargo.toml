# Workspace manifest for the trouve monorepo.
#
# Members:
#   crates/trouve-search   — code search/indexing library + CLI (published)
#   crates/trouve-*        — harness crates (protocol, core, providers, server, cli, ...)
#
# Every first-party artifact shares one release version and repository tag.
# See docs/adr/0012-single-version-monorepo-release-train.md.

[workspace]
resolver = "2"
members = ["crates/*"]

[workspace.package]
version = "4.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/jimsimon/trouve"
rust-version = "1.92"

[workspace.dependencies]
anyhow = "1.0.103"
async-trait = "0.1"
bytes = "1"
axum = { version = "0.8", features = ["macros"] }
chrono = { version = "0.4", features = ["serde"] }
dirs = "6.0.0"
futures = "0.3"
fontdb = "0.23.0"
fs4 = "0.13"
hmac = "0.12"
hex = "0.4"
jsonwebtoken = { version = "11.0", features = ["rust_crypto"] }
octocrab = "0.54"
reqwest = { version = "0.13", default-features = false, features = ["form", "json", "query", "stream", "rustls"] }
rusqlite = { version = "0.40", features = ["bundled"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.150", features = ["preserve_order"] }
sha2 = "0.10"
tempfile = "3.27.0"
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tokio-stream = { version = "0.1", features = ["sync"] }
toml = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
utoipa = { version = "5", features = ["chrono"] }
url = "2"
uuid = { version = "1", features = ["v4"] }
windows-sys = { version = "0.61.2", features = ["Win32_Storage_FileSystem"] }
xattr = "1.6.1"

# Internal crates
trouve-protocol = { path = "crates/trouve-protocol", version = "4.1.0" }
trouve-search = { path = "crates/trouve-search", version = "4.1.0" }
trouve-agents = { path = "crates/trouve-agents", version = "4.1.0" }
trouve-core = { path = "crates/trouve-core", version = "4.1.0" }
trouve-providers = { path = "crates/trouve-providers", version = "4.1.0" }
trouve-server = { path = "crates/trouve-server", version = "4.1.0" }
trouve-client-core = { path = "crates/trouve-client-core", version = "4.1.0" }
trouve-thread-view = { path = "crates/trouve-thread-view", version = "4.1.0" }
trouve-desktop-host = { path = "crates/trouve-desktop-host", version = "4.1.0" }
trouve-process = { path = "crates/trouve-process", version = "4.1.0" }

[profile.release]
lto = "thin"
codegen-units = 8

# Keep dev target dirs small: full DWARF debuginfo dominates target/ size.
# Line tables are enough for backtraces and panic locations; flip to
# `debug = true` locally if you need to step through code in a debugger.
[profile.dev]
debug = "line-tables-only"

# Dependencies get no debuginfo at all — we don't step through them.
[profile.dev.package."*"]
debug = false
